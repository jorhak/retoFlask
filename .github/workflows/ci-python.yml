name: Mi Pipeline con matrix

on:
  push:
    branches: [feature]
  pull_request:
    branches: [feature]

jobs:
  test-con-matrix:
    runs-on: self-hosted
    strategy:
      matrix:
        python-version: ['3.10', '3.8', '3.9']
    
    steps:
    - uses: actions/checkout@v4

    - uses: actions/setup-python@v4
      with:
        python-version: ${{matrix.python-version}}

    - run: pip install -r requirements.txt
    - run: pytest

    - name: Sincronizar con Postman
      uses: jneate/postman-collection-action@v1
      with:
        postmanApiKey: ${{ secrets.POSTMAN_KEY }}
        postmanWorkspaceId: ${{ vars.COLLECTION_ID }}
        postmanTimeout: 30000
